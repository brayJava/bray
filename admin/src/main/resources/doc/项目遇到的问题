1、使用分页插件PageHelper时，只能对下面需要执行的第一个mapper执行器起作用，后面的分页不起作用

2、th:onclick="'javascript:cerAudit('+${2}+',\''+${doctorDto.doctor.id}+'\')'"  如果在标签中click方法绑定需要调的函数
   函数参数有字符的情况下，需要用引号引起来

3、js点击事件，绑定元素时，如果页面元素还未渲染，则绑定不到元素，导致绑定失效。解决方法是加setTimeOut延时加载
    绑定。
